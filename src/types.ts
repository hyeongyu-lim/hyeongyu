export type Site = {
  NAME: string;
  TAGLINE: string;
  DEGREE: string;
  EDUCATION_PERIOD: string;
  INSTITUTION: string;
  EMAIL: string;
};

export type Metadata = {
  TITLE: string;
  DESCRIPTION: string;
};

export type Socials = {
  NAME: string;
  HREF: string;
}[];
